<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BreatheApp_Multimedia_EV">

        <!-- Meditación -->
        <activity
            android:name=".MeditacionActivity"
            android:exported="false" />

        <!-- Vídeos -->
        <activity
            android:name=".ListVideoActivity"
            android:exported="false" />
        <activity
            android:name=".ReproductorVideoActivity"
            android:exported="false" />

        <!-- Audios -->
        <activity
            android:name=".ListAudioActivity"
            android:exported="false" />
        <activity
            android:name=".ReproductorAudioActivity"
            android:exported="false" />

        <!-- Galería-->
        <activity
            android:name=".ListGalleryActivity"
            android:exported="false" />
        <activity
            android:name=".DetalleImagenActivity"
            android:exported="false" />

        <!-- Menús principales -->
        <activity
            android:name=".BibliotecaActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>